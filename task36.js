function make_shirt(size, message) {
    console.log("The shirt size is ".concat(size, " and it will have the message \"").concat(message, "\" printed on it."));
}
make_shirt("M", "Hello, World!"); // Output: The shirt size is M and it will have the message "Hello, World!" printed on it.
