let a: string = "ibrahim ghani";

console.log(`Lowercase: ${a.toLowerCase()}`);//It will convert the written satement into lowercase
console.log(`Uppercase: ${a.toUpperCase()}`);//It will convert the written satement into uppercase